# Metabase Dashboard Setup

1) Connect Metabase to Postgres database `datapipe`.
2) Set the data model to use the star schema in pipeline/schemas/star_schema.sql.
3) Create a dashboard matching pipeline/dashboards/time_series_dashboard.md.
4) Save the dashboard and export screenshots for CV.
